{
  "name": "vue-pack",
  "private": true,
  "scripts": {
    "test": "lerna exec -- yarn test"
  },
  "workspaces": [
    "packages/@gluons/*",
    "packages/*"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gluons/vue-pack.git"
  },
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}
