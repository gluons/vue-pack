{
  "name": "vue-pack",
  "private": true,
  "scripts": {
    "build": "lerna exec -- yarn build",
    "test": "lerna exec -- yarn test"
  },
  "workspaces": [
    "packages/@gluons/*",
    "packages/*"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gluons/vue-pack.git"
  },
  "devDependencies": {
    "lerna": "^3.0.3"
  }
}
